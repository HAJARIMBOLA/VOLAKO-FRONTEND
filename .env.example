# URL of the VOLAKO Spring Boot backend (server-side only, never exposed to the browser)
BACKEND_URL=http://localhost:8080

# Must match the app's own origin in production (used for the secure cookie flag)
NODE_ENV=development
